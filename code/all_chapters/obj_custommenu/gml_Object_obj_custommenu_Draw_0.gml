/// PATCH

/// REPLACE
        for (j = minj; j < maxj; j++)
        {
            if (m == menu)
            {
/// CODE
        for (j = minj; j < maxj; j++)
        {
            if (type == 1 && m == 0)
            {
                draw_set_color(optionTextColor[m][j])
            }
            else if (m == menu)
            {
/// END