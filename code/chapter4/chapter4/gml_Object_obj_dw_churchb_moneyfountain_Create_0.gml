/// PATCH

/// AFTER

if (global.flag[898] == 0)
{
    econ = 1;
/// CODE
    var tr = findspriteinfo(783);
    
    with (instance_create(tr.x, tr.y, obj_treasure_room))
        scr_size(2, 2);
/// END