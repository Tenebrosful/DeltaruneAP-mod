/// PATCH

/// AFTER
global.fe = 0;
global.interact = 1;
global.msc = 460;
/// CODE

if (room == room_field3)
    scr_checkspot(31);

if (room == room_forest_savepoint1)
{
    on = 1;
    scr_checkspot(32);
}

if (room == room_forest_savepoint2)
    scr_checkspot(33);

if (room == room_forest_fightsusie)
    scr_checkspot(34);
/// END