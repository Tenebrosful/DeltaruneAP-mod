/// PATCH

/// REPLACE
        global.flag[1312] += 1;
/// CODE
        
        if (room == room_dw_cliff_precipice)
            AP_sendLocation(296);
        
        if (room == room_dw_fcastle_trainroom)
            AP_sendLocation(323);
        
/// END